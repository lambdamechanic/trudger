# Trudgeable with hooks
# - Selects the next ready br task labeled "trudgeable".
# - On completion, removes the "trudgeable" label (task id is $1).
# - On requires-human, removes "trudgeable" and adds "human-required" (task id is $1).
codex_command: "codex --yolo exec"
commands:
  next_task: "br ready --json --label trudgeable --sort priority --limit 1 | jq -r 'if type == \"array\" and length > 0 then .[0].id // \"\" else \"\" end'"
  task_show: "br show"
  task_update_in_progress: "br update"
review_loop_limit: 5
log_path: "./.trudger.log"

hooks:
  on_completed: "bash -lc 'br label remove \"$1\" \"trudgeable\"'"
  on_requires_human: "bash -lc 'br label remove \"$1\" \"trudgeable\"; br label add \"$1\" \"human-required\"'"

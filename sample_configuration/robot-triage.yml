# Robot triage
# - Selects tasks via `bv --robot-next`.
# - No label changes (hooks are no-ops).
codex_command: "codex --yolo exec"
commands:
  next_task: "bv --robot-next | jq -r '.id // empty'"
review_loop_limit: 5
log_path: "./.trudger.log"

hooks:
  on_completed: "true"
  on_requires_human: "true"
